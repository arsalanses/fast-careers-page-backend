apiVersion: v1
kind: Service
metadata:
  name: {{ include "careers-helm.fullname" . }}
  labels:
    {{- include "careers-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "careers-helm.selectorLabels" . | nindent 4 }}
